import os

current_directory = os.getcwd()
data_directory = os.path.join(current_directory,'..', '..', 'data')
embed_dir = os.path.join(data_directory, 'output_embed/')
audio_dir = os.path.join(data_directory, 'songs/')